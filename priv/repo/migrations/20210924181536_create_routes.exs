defmodule Dway.Repo.Migrations.CreateRoutes do
  use Ecto.Migration

  def change do
    # create table(:routes) do
    #   add :api_token, :string
    #   add :drivers, :string
    #   add :order, :string

    #   timestamps()
    # end
  end
end
